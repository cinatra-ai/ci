Example connector that bridges an external email service into Cinatra. Use it to
send transactional email from your agents and workflows without managing SMTP
credentials by hand. Configure it once and every email-capable surface picks it up.

## Works with

- Agents that send email

## Capabilities

- Sends transactional email via the host email-send capability
- Resolves provider configuration from extension settings
