Example agent that drafts and sends follow-up email after a meeting. It reads the
meeting notes, composes a concise summary, and dispatches it through whichever
email connector the workspace has configured. No bespoke setup is required.

## Capabilities

- Drafts a meeting follow-up email from notes
- Sends it through the configured email-send capability
