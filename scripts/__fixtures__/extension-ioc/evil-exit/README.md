Adversarial fixture whose register THROWS (a real failure) after trying to forge a
passing fd-3 summary line without the parent nonce. The verdict is the worker exit
code, so a failing register cannot pass — the forged summary is ignored. This text
pads the README to the minimum byte length required by the marketplace card
contract so the README rule passes and only the probe verdict is under test here.

## Capabilities

- Throws during register (a real failure)
- Must be caught by the exit-code verdict
