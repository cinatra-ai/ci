# Example connector

Connect your workspace and Cinatra keeps the records in step. Every action runs
with the permissions you grant during install.

Reports are generated on demand from the records you select.
