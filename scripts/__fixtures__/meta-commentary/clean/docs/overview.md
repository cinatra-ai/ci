---
slug: example
title: Example integration
description: Connect the Example app to Cinatra.
navOrder: 1
tier: first-party
lifecycle: active
---

# Example integration

The Example integration connects your Example workspace to Cinatra so agents can
read and act on your data. Once installed, a connector exposes Example's records
as skills any connected agent can call as slash commands.

## What you can do

- Search records and open them from a chat.
- Create and update items through natural language.
- Share reusable prompts across your team.

Every action runs with the permissions you grant during install, and nothing
leaves your workspace unless you ask for it.

## Ordinary prose the AC5 patterns must NOT match (docs#156)

Each sentence below is real published product prose that a broader pattern
would have failed. They pin the RULED OUT decisions in the engine's policy
section — if a future pattern edit starts matching one of these, this fixture
turns red before a caller repo does.

- Background work keeps running and tells you when it lands.
- If you are landing here for the first time, start with the overview.
- You get a notification when an approval landed or a stage moved forward.
- The connector implements the ratified OAuth 2.1 specification.
- The design was ratified by the working group before the connector shipped.
- Bulk export is not yet supported, and scheduled runs are not yet available.
- See [#1620](https://github.com/cinatra-ai/cinatra/issues/1620) for the
  renderer design discussion.
- Per the settings you choose at install, the connector reads a narrower scope.
- Reports are generated on demand and never leave your workspace.
- Records you hand-edit in Example stay authoritative; Cinatra never overwrites
  them.
- No need to hand-edit field mappings; the connector maintains them for you.
- Events yet to land stay queued, and requests still in flight are allowed to
  finish before shutdown.
- Audit events are landing in a separate bucket from run events.
- See issue #123 for troubleshooting. If the webhook has not landed after five
  minutes, retry it.
- After the webhook landed, see issue #123 for troubleshooting.
- The connector follows the ratified W3C proposal, and the security policy was
  ratified by the standards committee.
- Following the decision tree, select the matching branch for your plan.
- Pick the delivery mode
- Access is ratified nightly for every workspace.
- The schema is ratified
- Mode selection stays per workspace.
- Delayed events are landing later than live ones.
- Algorithms ratified by NIST are available in FIPS mode.
- Only ratified algorithms run in FIPS mode.
- Algorithms ratified
  by NIST run in FIPS mode.
- Following the decision returned by the policy engine, retry the call.

Adjacent list items must never be joined across the wrap, whichever marker they
use:

+ Pick the delivery mode
+ Access is ratified nightly for every workspace.

1) Pick the delivery mode
2) Access is ratified nightly for every workspace.
