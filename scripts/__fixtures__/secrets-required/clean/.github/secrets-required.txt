# secrets-required-gate fixture: a repo in lockstep (gate passes).
# Names only; never a value.

ALPHA_TOKEN
  Purpose : example fixture secret referenced by ci.yml.

DOCKERHUB_USERNAME / DOCKERHUB_TOKEN
  Purpose : example multi-name line; both are referenced by ci.yml.
