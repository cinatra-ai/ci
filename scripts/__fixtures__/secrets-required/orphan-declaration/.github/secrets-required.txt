# secrets-required-gate fixture: an ORPHAN DECLARATION (gate fails).
# STALE_TOKEN is declared but no workflow references it.

ALPHA_TOKEN
  Purpose : declared and referenced.

STALE_TOKEN
  Purpose : declared but referenced by no workflow => orphan declaration.
