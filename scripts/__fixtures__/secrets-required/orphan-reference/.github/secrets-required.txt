# secrets-required-gate fixture: an ORPHAN REFERENCE (gate fails).
# ci.yml references DEV_LOCK_BUMP_TOKEN, which is missing here — the exact
# recurrence the release-governance drift gates set out to prevent.

ALPHA_TOKEN
  Purpose : declared and referenced.
