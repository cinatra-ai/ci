# Self-caller for the org baseline gitignore gate — see self-actions-pinned-gate.yml
# for why this repo needs its own thin callers.
name: gitignore-gate

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  gitignore-gate:
    uses: cinatra-ai/ci/.github/workflows/gitignore-gate.yml@4c903fe9ca71ad47752a2bc92b72893e5f7c9814 # 0.1.0
    with:
      ref: 4c903fe9ca71ad47752a2bc92b72893e5f7c9814
